import { generateText } from "ai";
import { createLovableAiGatewayProvider } from "./ai-gateway.server";
export async function buildKiGrounding(userId:string){const{ supabaseAdmin:db}=await import("@/integrations/supabase/client.server");const[{data:trades},{data:plans},{data:alerts},{data:health},{data:candles},{data:metrics}]=await Promise.all([(db as any).from("market_inventory_trades").select("*").eq("user_id",userId).order("created_at",{ascending:false}).limit(100),(db as any).from("ki_position_plans").select("*").eq("user_id",userId).eq("active",true),(db as any).from("ki_operator_alerts").select("alert_type,severity,title,message,created_at").eq("user_id",userId).is("acknowledged_at",null).limit(20),(db as any).from("market_intelligence_feed_health").select("*"),(db as any).from("market_intelligence_candles").select("exchange,fiat,side,interval_seconds,bucket_at,close,executable_price,depth_asset,merchant_count,volatility").eq("interval_seconds",300).order("bucket_at",{ascending:false}).limit(240),(db as any).from("ki_model_metrics").select("*").order("metric_date",{ascending:false}).limit(40)]);return{generated_at:new Date().toISOString(),open_and_recent_trades:trades??[],active_operator_plans:plans??[],active_alerts:alerts??[],feed_health:health??[],recent_5m_market:candles??[],model_metrics:metrics??[]};}
export function buildKiSystem(grounding:unknown){return `You are Waides KI, a disciplined veteran P2P market operator and decision-support analyst. You never execute trades, move funds, promise profit, or claim certainty. Be concise and skeptical of apparent spreads. Consider executable depth, merchant quality, payment rails, settlement, all fees, transfer risk, bank risk, stale data, and opportunity cost. Deterministic operator plans are authoritative for calculations. Never invent prices or probabilities. If confidence_eligible is false, say the system is calibrating. For position advice state action, venue, executable price and amount, break-even, expected net, evidence, invalidation condition, next check, and missing data. Refuse time-sensitive advice when feeds are stale.\nUSER_AND_MARKET_DATA:\n${JSON.stringify(grounding).slice(0,50000)}`;}
export async function answerKi(userId:string,text:string){const key=process.env.LOVABLE_API_KEY;if(!key)throw new Error("Missing LOVABLE_API_KEY");const grounding=await buildKiGrounding(userId);const gateway=createLovableAiGatewayProvider(key);return(await generateText({model:gateway("google/gemini-2.5-flash"),system:buildKiSystem(grounding),prompt:text})).text;}
